import React,{createContext,useContext,useEffect,useState}from'react';import {api}from'../api/api';import{clearSession,getSession,saveSession}from'../storage/authStorage';const C=createContext();export const useAuth=()=>useContext(C);export function AuthProvider({children}){const[session,setSession]=useState(null),[loading,setLoading]=useState(true);useEffect(()=>{(async()=>{const s=await getSession();if(s)try{const{data}=await api.get('/auth/me');setSession({...s,usuario:data.data})}catch{await clearSession()}setLoading(false)})()},[]);const enter=async(path,body)=>{const{data}=await api.post(path,body);await saveSession(data.data);setSession(data.data)};const logout=async()=>{await clearSession();setSession(null)};return <C.Provider value={{session,loading,login:(e,p)=>enter('/auth/login',{email:e,password:p}),register:b=>enter('/auth/register',b),logout}}>{children}</C.Provider>}
