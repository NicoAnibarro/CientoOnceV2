import axios from 'axios';import {getSession} from '../storage/authStorage';export const api=axios.create({baseURL:process.env.EXPO_PUBLIC_API_URL,timeout:12000});api.interceptors.request.use(async config=>{const s=await getSession();if(s?.token)config.headers.Authorization=`Bearer ${s.token}`;return config});export const message=e=>e.response?.data?.mensaje||e.message||'Ocurrió un error';
