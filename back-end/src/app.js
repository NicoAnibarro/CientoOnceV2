require('dotenv').config();const express=require('express'),cors=require('cors'),helmet=require('helmet'),db=require('./config/db'),routes=require('./routes'),errors=require('./middleware/error.middleware');const app=express();app.use(helmet());app.use(cors({origin:process.env.CORS_ORIGIN==='*'?'*':(process.env.CORS_ORIGIN||'').split(',')}));app.use(express.json({limit:'1mb'}));app.get('/api/health',(req,res)=>res.json({ok:true,mensaje:'API disponible'}));app.get('/api/test-db',async(req,res,next)=>{try{await db.query('SELECT 1');res.json({ok:true,mensaje:'Conexión MySQL correcta'})}catch(e){next(e)}});app.use('/api',routes);app.use((req,res)=>res.status(404).json({ok:false,mensaje:'Ruta no encontrada'}));app.use(errors);module.exports=app;
